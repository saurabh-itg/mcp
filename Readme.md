# MCP File Assistant

A simple demonstration of an MCP project for working with local system files. It shows how to use tool calls with MCP servers.

## Running the Project

#activate environment 

source ../.venv/bin/activate

To run the server:

```bash
python3 server.py
```

To run the client:

```bash
python3 client.py
```
